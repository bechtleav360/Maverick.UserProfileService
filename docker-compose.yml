version: '3.7'

services:
  mkdocs-server:
    build:
      context: .
      dockerfile: Dockerfile
      tags:
        - "mkdocs:container"
    ports:
      - 8000:8000
    volumes:
      - ".:/docs"
  
